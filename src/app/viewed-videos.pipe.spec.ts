import { ViewedVideosPipe } from './viewed-videos.pipe';

describe('ViewedVideosPipe', () => {
  it('create an instance', () => {
    const pipe = new ViewedVideosPipe();
    expect(pipe).toBeTruthy();
  });
});
